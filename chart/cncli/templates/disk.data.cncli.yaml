apiVersion: v1
kind: PersistentVolume
metadata:
  name: "{{ .Values.namespace }}-data-cncli-pv"
  namespace: {{ .Values.namespace }}
spec:
  storageClassName: ""
  claimRef:
    name: "{{ .Values.namespace }}-data-cncli-pvc"
    namespace: {{ .Values.namespace }}
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: nfs-server-svc.atlas-raddcreative-io.svc.cluster.local
    path: "/"
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: "{{ .Values.namespace }}-data-cncli-pvc"
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: "{{ .Values.namespace }}-data-cncli-pv"
  resources:
    requests:
      storage: 2Gi
